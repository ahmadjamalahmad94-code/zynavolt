<?xml version="1.0" encoding="utf-8"?>
<!-- Cold-start splash: solid navy backdrop matching the Zynavolt
     logo's own background so there's no white flash before Flutter
     paints its own splash widget. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
